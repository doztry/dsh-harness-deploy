# dsh-web-next

Custom / replacement web UI plugin direction for dsh (center column, connectors, contacts).

Status: **scaffold**. Build as dsh client plugins against existing host APIs (`/__connectors/*`, sessions, agents).
