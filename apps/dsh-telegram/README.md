# dsh-telegram

Telegram-focused dsh: same `ctx.agents` harness, **no full web product UI**.

Status: **scaffold**. Next: slim `start.mjs` that boots dsh headless + only `dsh-connectors` channel + health.

Until then, use **dsh-full** with Telegram token set; web UI remains available but channel path is already agent-native.
